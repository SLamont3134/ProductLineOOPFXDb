/**
 * The MonitorType enum. The acceptable monitor types for the product
 *
 * @author Sean Lamont
 * @date 10/8/19
 */
package lamont;
// conflict between CheckStyle and google format.

/** Create an enum called MonitorType that will store. */
public enum MonitorType {
  LCD(),
  LED()
}
